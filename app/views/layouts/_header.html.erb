<header>
    <nav class="navbar navbar-custom navbar-fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">Titre du Site</a>
            <p class="navbar-text navbar-right">
                <% if user_signed_in? %>
                    Bienvenue <strong><%= current_user.email %></strong> !
                    <%= link_to 'Mon dashboard', "/", :class => 'navbar-link' %> |
                    <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, :class => 'navbar-link-custom'  %>
                <% else %>
                    <%= link_to "Se connecter", new_user_session_path, :class => 'navbar-link-custom'  %>
                <% end %>
            </p>
        </div>
    </nav>
</header>